{
  "mcpServers": {
    "continuity": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/mcp.mjs"],
      "env": {
        "OPENROUTER_API_KEY": "${user_config.openrouter_api_key}"
      }
    }
  }
}
