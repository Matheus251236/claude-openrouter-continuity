{
  "name": "claude-openrouter-continuity",
  "version": "0.2.0-experimental",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs"
  }
}
